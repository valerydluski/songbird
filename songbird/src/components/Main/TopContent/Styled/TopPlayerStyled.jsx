import styled from 'styled-components';

const TopPlayerStyled = styled.div`
  grid-area: player;
  display: flex;
  place-self: center;
  width: 90%;
  &div div div div {
    color: #fff !important;
  }
`;

export default TopPlayerStyled;
