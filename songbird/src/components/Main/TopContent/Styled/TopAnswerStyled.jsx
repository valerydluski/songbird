import styled from 'styled-components';

const TopAnswerStyled = styled.div`
  color: #6550de;
  font-size: 30px;
  grid-area: answer;
  display: flex;
  place-self: center;
  font-style: normal;
  font-weight: bold;
`;

export default TopAnswerStyled;
