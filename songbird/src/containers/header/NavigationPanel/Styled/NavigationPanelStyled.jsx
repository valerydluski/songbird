import styled from 'styled-components';

const NavigationPanelStyled = styled.ul`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  border-radius: 5px;
  overflow: hidden;
`;

export default NavigationPanelStyled;
